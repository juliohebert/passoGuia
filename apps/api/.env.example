# Copie para .env e ajuste se necessário. `docker compose up -d` (neste diretório)
# sobe um Postgres local com essas mesmas credenciais.
DATABASE_URL="postgresql://passoguia:passoguia@localhost:5432/passoguia?schema=public"
